<view class="userDetail">
  <form catchsubmit="formSubmit" catchreset="formReset">
    <view class="form-content">
      <!--<view class="form_table">
        <view class="form_input">昵称</view>
        <input class="weui-input" value="{{userInfoDetail.nickName}}" name="name" placeholder="昵称" />
      </view>-->
      <view class="form_table">
        <view class="form_input">性别</view>
        <!-- <picker class="picker" bindchange="bindPickerChange" value="{{index}}" range="{{genderList}}">
          <view class="picker">
            当前选择：{{array[index]}}
          </view>
        </picker> -->
        <input class="weui-input" value="{{list.data[0].sex}}" name="sex" placeholder="性别" />
      </view>
      <!--<view class="form_table">
        <view class="form_input">年龄</view>
        <input class="weui-input" value="{{list.data[0].age}}" name="age" placeholder="年龄" />
      </view>-->
      <view class="form_table">
        <view class="form_input">生日</view>
        <input class="weui-input" value="{{list.data[0].birthday}}" name="birthday" placeholder="生日" />
      </view>
      <view class="form_table">
        <view class="form_input">联系方式</view>
        <input class="weui-input" value="{{list.data[0].phone}}" name="phone" placeholder="联系方式" />
      </view>
      <view class="form_table">
        <view class="form_input">个人简介</view>
        <input class="weui-input" value="{{list.data[0].profile}}" name="profile" placeholder="个人简介" />
      </view>
      
      <!-- <view class="submit" formType="submit">编辑资料</view> -->
        <view class="submit_button">
          <button class="submit" formType="submit">确定</button>
        </view>
    </view>
  </form>
</view>