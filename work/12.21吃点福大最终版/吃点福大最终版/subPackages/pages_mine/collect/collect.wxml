<view class="like">
    <view class="user_text">
       <!-- <view>收藏的菜</view>
        <view class="backbtn" bindtap="backClick">{{'<'}}返回</view>-->
      </view>
    <product-list wx:for="{{productList}}" wx:key="item" item="{{item}}" productType="collect"
    data-index="{{index}}"
    bind:handleClick='handleClick'
    bind:deleteClick='deleteClick'
    ></product-list>
</view>